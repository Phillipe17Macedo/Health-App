import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 120,
    height: 150,
    marginTop: 25,
    backgroundColor: 'gray',
    borderRadius: 10,
    flexDirection: 'row',
    
  },
});